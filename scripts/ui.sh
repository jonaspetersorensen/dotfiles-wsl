#!/bin/bash


# DESCRIPTIOM
# One file to rule them all, One file to find them,
# One file to bring them all and in the darkness bind them
# In the Land of radix-boot where the ui lie


# Load ui variabels and functions
. "$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )/ui_colors.sh"
. "$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )/ui_print.sh"


